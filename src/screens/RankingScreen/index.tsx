import React from 'react';
import {View, Text} from 'react-native';

const RankingScreen = () => {
    return (
        <view style={{flex: 1, justifyContent:'center',alignItems:'center'}}>
            <Text>It's a RankingScreen Component</Text>
        </view>
    );
};

export default RankingScreen;