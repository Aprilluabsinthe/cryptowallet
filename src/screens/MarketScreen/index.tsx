import React from 'react';
import {View, Text} from 'react-native';

const MarketScreen = () => {
    return (
        <view style={{flex: 1, justifyContent:'center',alignItems:'center'}}>
            <Text>It's a MarketScreen Component</Text>
        </view>
    );
};

export default MarketScreen;